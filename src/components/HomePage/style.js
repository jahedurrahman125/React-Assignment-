import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    root: {
        flexGrow: 1,
        margin: "auto",
        width: "50%",
      },
      heading:{
        fontVariant: "h1",
        fontWeight: "bold",
        textAlign: "center",
        padding: "2vh"
      }
}));